#!/usr/bin/env ruby
#^syntax detection

# use dependencies defined in metadata.json
metadata

# Modules we have patched and are waiting on a merge
mod 'puppetlabs-apt',
    :git => 'https://github.com/puppetlabs/puppetlabs-apt.git',
    :ref => 'master'
